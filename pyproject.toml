[project]
name = "mcp-aseps"
version = "0.1.0"
description = "Modular multi-server MCP suite: core tools, memory, knowledge, skills, and integrations."
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=3.0",
    "pydantic>=2.0",
    "pydantic-settings>=2.0",
    "httpx>=0.27",
    "psycopg[binary]>=3.2",
    "psycopg-pool>=3.2",
    "redis>=5.0",
    "aiosqlite>=0.22",
    "sqlite-vec>=0.1.9",
    "sqlalchemy[asyncio]>=2.0",
    "python-telegram-bot>=21.0",
    "google-auth>=2.0",
    "google-auth-httplib2>=0.2",
    "google-api-python-client>=2.0",
    "openai>=1.0",
    "pyyaml>=6.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
    "ruff>=0.6",
    "mypy>=1.11",
]

[project.scripts]
mcp-core-server = "servers.core.server:main"
mcp-memory-server = "servers.memory.server:main"
mcp-knowledge-server = "servers.knowledge.server:main"
mcp-skills-server = "servers.skills.server:main"
mcp-gmail-bridge = "servers.bridge.gmail_server:main"
mcp-telegram-bridge = "servers.bridge.telegram_server:main"
mcp-gemini-bridge = "servers.bridge.gemini_server:main"
mcp-vision-bridge = "servers.bridge.vision_server:main"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.mypy]
python_version = "3.12"
strict = true
exclude = [".venv", "archive", "external"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = [".", "servers", "shared"]

[tool.setuptools.packages.find]
include = ["shared", "servers*"]
